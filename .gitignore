# build output
dist/
.astro/

# приватные артефакты поиска работы / писем (репо публичный — не коммитим)
out/
# подобранные под вакансию таргеты не публикуем по умолчанию;
# чтобы опубликовать конкретный — `git add -f targets/tailored-<slug>.yaml`
targets/tailored-*.yaml

# dependencies
node_modules/

# secrets / env
.env
.env.*
!.env.example

# editor / os
.DS_Store
*.log
.vscode/
.idea/

# playwright
/test-results/
/playwright-report/

# playwright mcp artifacts
.playwright-mcp/
