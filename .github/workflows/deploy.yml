name: Deploy resume to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Один деплой за раз; новый пуш отменяет предыдущий.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Configure Pages
        id: pages
        uses: actions/configure-pages@v6

      - run: npm ci

      - name: Validate content
        run: npm run validate

      # site/base берутся из дефолтов astro.config.mjs (kusokbanana.ru, base '/').
      # configure-pages для кастомного домена ненадёжно отдавал base '/resume',
      # поэтому на его outputs больше не полагаемся.
      - name: Build HTML
        run: npm run build

      - name: Generate MD + JSON + TXT
        run: |
          npm run build:md
          npm run build:json
          npm run build:txt

      - name: Install Chromium for PDF
        run: npx playwright install --with-deps chromium

      - name: Generate PDF
        run: npm run build:pdf

      - name: Generate OG image
        run: npm run build:og

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
